# Production builds are served by FastAPI from the same origin as the API,
# so a relative URL keeps working whatever hostname the site is reached through
# (localhost, the Tailscale MagicDNS name, or anything later).
VITE_API_URL=/api
